
   <a href="#comments" class="comment-bubble">
   {if comment_total == 1}
      1 comment
   {if:elseif comment_total > 1}
      <span class="comment-count-js" data-url-title="{url_title}">{comment_total}</span> comments
   {if:elseif allow_comments}
      no comments yet
   {if:else}
      no comments
   {/if}
   </a>