
   {if allow_comments}
   <a href="#comments" class="comment-bubble">
   {if comment_total == 1}
      1 comment
   {if:elseif comment_total > 1}
      {comment_total} comments
   {if:else}
      no comments yet
   {/if}
   </a>
   {/if}